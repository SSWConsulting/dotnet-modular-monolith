namespace Common.Endpoints.Interfaces;

/// <summary>
/// Marker interface to auto-register endpoints.  Can implement this as many times as possible (e.g. for each API group)
/// </summary>
public interface IMapEndpoints
{
    // public void MapEndpoints(WebApplication app);
}
